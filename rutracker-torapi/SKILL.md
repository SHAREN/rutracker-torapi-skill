---
name: rutracker-torapi
description: Поиск и загрузка торрентов (фильмы, сериалы, софт, Linux-образы и другие файлы) через TorAPI провайдеры RuTracker, Kinozal, RuTor и NoNameClub. Используй, когда пользователь просит найти торрент, получить magnet/.torrent или добавить раздачу в qBittorrent WebUI с подтверждением.
user-invocable: true
metadata:
  {
    "openclaw": {
      "emoji": "📥",
      "requires": { "bins": ["curl", "jq"] },
      "env": ["QB_WEB_URL", "QB_USERNAME", "QB_PASSWORD"],
      "install": [
        {
          "id": "qbittorrent",
          "kind": "manual",
          "label": "Убедись, что qBittorrent запущен с включённым WebUI (по умолчанию http://127.0.0.1:8080)"
        }
      ]
    }
  }
---
# TorAPI Multi-Provider (RuTracker, Kinozal, RuTor, NoNameClub)

Когда использовать:
- Пользователь просит найти или скачать фильм, сериал, музыку, софт, игру, Linux-образ или другой торрент-контент
- Нужно быстро получить magnet-ссылку или .torrent
- Нужно добавить выбранную раздачу в qBittorrent WebUI

Основные команды:
1. Поиск по названию (всегда через локальный скрипт с сортировкой по сидам):
`bash scripts/search-torrents-sorted.sh "НАЗВАНИЕ" 10 0 rutracker`
`bash scripts/search-torrents-sorted.sh "НАЗВАНИЕ" 10 0 kinozal`
`bash scripts/search-torrents-sorted.sh "НАЗВАНИЕ" 10 0 rutor`
`bash scripts/search-torrents-sorted.sh "НАЗВАНИЕ" 10 0 nonameclub`

2. Детали по ID (получить magnet/hash/.torrent):
`curl -s "https://torapi.vercel.app/api/search/id/<provider>?query=ID" | jq`
где `<provider>`: `rutracker | kinozal | rutor | nonameclub`

3. Добавить торрент в qBittorrent «умным» сценарием (рекомендуется всегда):
`bash scripts/add-series-smart.sh --id ID`
или
`bash scripts/add-series-smart.sh --magnet "magnet:?xt=urn:btih:..."`

Fast-path (меньше лишних вычислений и токенов):
- если размер уже известен из карточки/поиска, передай его сразу:
`bash scripts/add-series-smart.sh --id ID --size-bytes 7290000000 --name "Название релиза"`
- в этом режиме скрипт не пересчитывает размер по списку файлов TorAPI.

Скрипт `add-series-smart.sh` делает за один проход:
- логин в qBittorrent,
- проверку свободного места (`размер торрента + 5 ГБ`),
- добавление в обычном режиме или в паузе при нехватке места,
- включение `sequentialDownload` и `firstLastPiecePrio`,
- приоритет серий (1-я max, 2-я high, остальные normal),
- стандартизованный JSON-отчёт (`input_mode`, `state`, `added_paused`, `series_priority_plan` и т.д.).

Формат ответа пользователю (обязательно после поиска):
- Сначала сделай запрос поиска по названию через `scripts/search-rutracker-sorted.sh` (это обязательно).
- Скрипт уже возвращает результаты, отсортированные по количеству сидов (по убыванию, больше сидов выше).
- Верни список минимум из 3 вариантов (или меньше, если найдено меньше).
- Для каждого варианта используй красивый единый шаблон:
  - `#N. <Название 1>`
  - `Год: <год или н/д>`
  - `Разрешение/качество: <качество/разрешение или н/д>`
  - `Размер: <вес>`
  - `Сиды/пиры: <seeds>/<peers>`
  - `Озвучка: <озвучка или н/д>`
  - `Серии: <например 1-4 из 6, или Полный сезон, или н/д>` (обязательно для сериалов)
- В «Название 1» выводи чистое основное название релиза (без лишнего мусора, если возможно).
- Для сериалов обязательно извлекай и показывай количество серий/полноту сезона из названия (`Серии: X-Y из Z`, `X из Z`, `Полный сезон` и т.п.).
- Если год/качество/озвучка/серии не выделяются надёжно, ставь `н/д`, но не пропускай поле.
- После списка предложи выбор: «Какой вариант добавить в qBittorrent?»

Примеры использования:
Пользователь: «Найди The Rookie 6 сезон на rutracker»
→ Сделай поиск
→ Покажи топ-3 результата (название, год, качество/разрешение, размер, сиды, серии, magnet)
→ Спроси «Добавить в qBittorrent?»
→ После подтверждения добавь выбранный.

Пользователь: «Скачай [название]»
→ Найди релевантные раздачи
→ Покажи лучший(ие) вариант(ы) по сидами/качеству
→ Добавь только после явного подтверждения.

Правила безопасности:
Всегда показывай пользователю название, размер и количество сидов перед добавлением.
Никогда не добавляй без явного подтверждения «да/добавь».
Если QB_WEB_URL не указан — верни magnet-ссылку без добавления.
Используй только публичный TorAPI (https://torapi.vercel.app).

Проверка свободного места (обязательно перед началом скачивания):
1. Перед добавлением раздачи проверь свободное место через qBittorrent API:
   - `GET $QB_WEB_URL/api/v2/sync/maindata` → поле `server_state.free_space_on_disk`.
2. Определи размер выбранного торрента (из результата поиска/деталей).
3. Рассчитай минимум: `требуемо = размер_торрента + 5 ГБ запаса`.
4. Если `free_space_on_disk >= требуемо`:
   - Добавляй торрент в обычном режиме (без паузы).
5. Если `free_space_on_disk < требуемо`:
   - Добавляй торрент только в паузе через API qBittorrent:
     - `POST $QB_WEB_URL/api/v2/torrents/add`
     - параметры: `urls=<MAGNET>` и `paused=true`
     - пример:
       - `curl -s -b /tmp/qb.cookie -X POST "$QB_WEB_URL/api/v2/torrents/add" --data-urlencode "urls=magnet:?xt=urn:btih:..." --data-urlencode "paused=true"`
   - После добавления проверь состояние через `GET $QB_WEB_URL/api/v2/torrents/info` и убедись, что статус `pausedDL`/`pausedUP`.
   - Если добавление в паузе не удалось — НЕ добавляй торрент вообще.
6. Для сериалов и фильмов после добавления торрента включай оба режима:
   - `sequentialDownload = true` (Загружать последовательно)
   - `firstLastPiecePrio = true` (Загружать первую и последнюю часть в первую очередь)
   Как сделать через qBittorrent API:
   - `POST $QB_WEB_URL/api/v2/torrents/toggleSequentialDownload` с `hashes=<HASH>`
   - `POST $QB_WEB_URL/api/v2/torrents/toggleFirstLastPiecePrio` с `hashes=<HASH>`
   - Перед переключением желательно проверить текущее состояние торрента, чтобы не выключить флаг повторным вызовом; цель — чтобы оба флага были включены (`true`).

7. Если пользователь скачивает сериал (многофайловая раздача с сериями), после добавления торрента выставляй приоритет файлов:
   - 1-я серия: максимальный приоритет
   - 2-я серия: высокий приоритет
   - 3-я и все остальные: обычный приоритет
   Как сделать через qBittorrent API:
   - Получить список файлов: `GET $QB_WEB_URL/api/v2/torrents/files?hash=<HASH>`
   - Найти индексы файлов серий по порядку (обычно по имени/номеру серии)
   - Установить приоритеты:
     - 1-я серия: `POST $QB_WEB_URL/api/v2/torrents/filePrio` с `id=<index1>&priority=7`
     - 2-я серия: `POST $QB_WEB_URL/api/v2/torrents/filePrio` с `id=<index2>&priority=6`
     - остальные: `POST $QB_WEB_URL/api/v2/torrents/filePrio` с `id=<indexes>&priority=1`

8. Всегда сообщай пользователю в ответе:
   - свободное место,
   - размер торрента,
   - требуемый минимум (размер + 5 ГБ),
   - итоговое действие (добавлен / добавлен на паузе / не добавлен),
   - если это сериал: какие приоритеты файлов выставлены.

Переменные окружения (добавь в ~/.openclaw/openclaw.json):
"skills": {
  "entries": {
    "rutracker-torapi": {
      "enabled": true,
      "env": {
        "QB_WEB_URL": "http://127.0.0.1:8080",
        "QB_USERNAME": "admin",
        "QB_PASSWORD": "твой_пароль"
      }
    }
  }
}
